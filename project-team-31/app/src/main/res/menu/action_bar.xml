<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
            android:id="@+id/action_bar_menu_switch"
            android:title="Focus Mode"
            app:actionLayout="@layout/action_bar_menu_switch"
            app:showAsAction="always"
            app:actionViewClass="android.widget.Switch" />

    <item android:id="@+id/action_bar_menu_swap_views"
            android:title="@string/swap_views"
            android:icon="@drawable/drop_down"
            app:iconTint="@color/white"
            app:showAsAction="always">

            <menu>
                <item
                        android:id="@+id/today"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:title="today" />
                <item
                        android:id="@+id/tomorrow"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:title="tomorrow" />
                <item
                        android:id="@+id/pending"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:title="pending" />

                <item
                        android:id="@+id/recurrent"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:title="recurrent" />
            </menu>
    </item>

</menu>